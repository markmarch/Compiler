# Postfix expressions
main = fun () -> int {
  a = length("a" + "b");
  b = 12 : string;
  c = (x = 1, y = 2);
  d = c.x;
  e = [1, 2];
  f = e[0];
  g = [ (x = 1, y = 2), (x = 3, y = 4) ];
  h = length(g[1].y : string);
  print(h + "\n");
  -> 0;
}
